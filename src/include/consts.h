#pragma once

constexpr unsigned int MAX_ITER = 256;        // Good default value
constexpr unsigned int FRAMES = 300;         // How long / fluent do you want your animation?
constexpr unsigned int CYCLE_FRAMES = 300;   // When does the rotational part repeat?
constexpr unsigned int WIDTH = 1920;          // Increase WIDTH/HEIGHT for better results
constexpr unsigned int HEIGHT = 1080;         //    or decrease for faster renders.
